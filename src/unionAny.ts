let subs: number | string = '1M'

let apiRequestStatus: 'pending' | 'success' | 'error' = 'pending';
let airlineSeat: 'aisle' | 'window' | 'middle' = 'middle';

// type union = when we explisityle define what data type we want to use.
// type any = variable doesn't card what data type of data you will set in future.